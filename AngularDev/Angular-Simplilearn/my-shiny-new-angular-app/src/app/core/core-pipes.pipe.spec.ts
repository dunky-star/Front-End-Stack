import { CorePipesPipe } from './core-pipes.pipe';

describe('CorePipesPipe', () => {
  it('create an instance', () => {
    const pipe = new CorePipesPipe();
    expect(pipe).toBeTruthy();
  });
});
